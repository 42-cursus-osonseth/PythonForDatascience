# ft_package

A small Python package for practicing package creation and distribution.

## Installation

You can install the package locally using pip:

```bash
pip install ./dist/ft_package-0.0.1-py3-none-any.whl
